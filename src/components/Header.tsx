"use client";
import Image from "next/image";
import Link from "next/link";
import appIcon from "@/app/icon.png";
export function Header({onLocate,locating,hasLocation}:{onLocate:()=>void;locating:boolean;hasLocation:boolean}) { return <header className="border-b border-white/10 bg-[#06455d] text-white"><div className="mx-auto flex max-w-6xl items-center justify-between px-4 py-4 sm:px-6"><Link href="/" className="flex items-center gap-2.5" aria-label="Playa Hoy, inicio"><Image src={appIcon} alt="" width={36} height={36} className="size-9 shrink-0 rounded-xl"/><span className="font-[family-name:var(--font-display)] text-lg font-extrabold tracking-tight">Playa Hoy</span></Link><button onClick={onLocate} disabled={locating} className="flex min-h-10 items-center gap-2 rounded-full border border-white/20 bg-white/10 px-3.5 text-sm font-bold transition hover:bg-white/20 disabled:opacity-70"><span aria-hidden>⌖</span><span className="hidden sm:inline">{locating?"Localizando…":hasLocation?"Ubicación activa":"Usar mi ubicación"}</span><span className="sm:hidden">{locating?"Localizando…":hasLocation?"Ubicación activa":"Ubicarme"}</span></button></div></header>; }
