import type { Beach } from "@/lib/types";
import { BeachCard } from "./BeachCard";
export type RankedBeach={beach:Beach;score:number;distance?:number};
export function BeachRanking({beaches,timeLabel}:{beaches:RankedBeach[];timeLabel?:string}) { return <section aria-labelledby="ranking-title"><div className="mb-4 flex items-end justify-between"><div><p className="mb-1 text-xs font-extrabold uppercase tracking-[.16em] text-[#078679]">{timeLabel?`Ranking · ${timeLabel}`:"Ranking de ahora"}</p><h2 id="ranking-title" className="text-2xl font-extrabold tracking-tight">Mejores playas {timeLabel?`a las ${timeLabel}`:"ahora"}</h2></div><span className="text-sm font-bold text-[#647b86]">{beaches.length} opciones</span></div>{beaches.length?<div className="grid gap-3">{beaches.map((item,index)=><BeachCard key={item.beach.id} {...item} position={index+1}/>)}</div>:<div className="rounded-2xl border border-[#dce5e4] bg-white p-6 text-sm leading-6 text-[#4c6b73]">No hay playas recomendables con las condiciones previstas para este momento.</div>}</section>; }
