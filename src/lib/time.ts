export function formatDataAge(updatedAt?:string){if(!updatedAt)return"Sin fecha disponible";const date=new Date(updatedAt);if(Number.isNaN(date.getTime()))return"Sin fecha disponible";const minutes=Math.max(0,Math.round((Date.now()-date.getTime())/60000));if(minutes<2)return"actualizado ahora";if(minutes<60)return`actualizado hace ${minutes} min`;const hours=Math.round(minutes/60);if(hours<24)return`actualizado hace ${hours} h`;return`último control ${new Intl.DateTimeFormat("es-ES",{day:"2-digit",month:"2-digit",year:"numeric"}).format(date)}`}
